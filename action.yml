name: 'Hello World'
description: 'Greet someone'
# inputs:
#   who-to-greet:  # id of input
#     description: 'Who to greet'
#     required: true
#     default: 'World'
# outputs:
#   random-number:
#     description: "Random number"
#     value: ${{ steps.random-number-generator.outputs.random-number }}
runs:
  using: "composite"
  steps:
#     - run: echo "${{ github.action_path }}" >> $GITHUB_PATH
#       shell: bash 
    - run: chmod +x ${{ github.action_path }}/bye.sh
      shell: bash
    - run: chmod +x ${{ github.action_path }}/README.md
      shell: bash
    - run: echo "some data for the file" >> ${{ github.action_path }}/README.md
      shell: bash
    - run: ${{ github.action_path }}/bye.sh
      shell: bash
